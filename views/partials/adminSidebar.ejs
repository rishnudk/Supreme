
	<!-- Boxicons CSS (for icons styling) -->
<!-- <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'> -->

<!-- Boxicons JavaScript (for animated icons, etc.) -->
<script src="https://unpkg.com/boxicons@2.1.4/dist/boxicons.js"></script>


<!-- Admin Dashboard Link (Visible Always) -->


<!-- Navbar Toggle Button -->
<button class="admin-navbar-toggler" id="adminToggler">
    <span class="admin-navbar-toggler-icon">&#9776;</span>
</button>

<!-- SIDEBAR -->
<section id="admin-sidebar">
    <ul class="admin-side-menu">
        <li>
            <a href="/admin/salesReport">
                <i class='bx bxs-dashboard bx-sm'></i>
                <span class="text">Dashboard</span>
            </a>
        </li>
        <li>
            <a href="/admin/user">
                <i class='bx bxs-user bx-sm'></i>
                <span class="text">User</span>
            </a>
        </li>
        <li>
            <a href="/admin/category">
                <i class='bx bxs-category bx-sm'></i>
                <span class="text">Category</span>
            </a>
        </li>
        <li>
            <a href="/admin/products">
                <i class='bx bxs-box bx-sm'></i>
                <span class="text">Products</span>
            </a>
        </li>
        <li>
            <a href="/admin/products/inventory">
                <i class='bx bxs-box bx-sm'></i>
                <span class="text">Inventory</span>
            </a>
        </li>
        <li>
            <a href="/admin/orders">
                <i class='bx bxs-group bx-sm'></i>
                <span class="text">Orders</span>
            </a>
        </li>
         <li>
            <a href="/admin/coupons">
                <i class='bx bxs-group bx-sm'></i>
                <span class="text">Coupons</span>
            </a>
        </li>  <li>
            <a href="/admin/Offers">
                <i class='bx bxs-group bx-sm'></i>
                <span class="text">offers</span>
            </a>
        </li>
        <li>
            <a href="/admin/return-requests">
                <i class='bx bxs-cog bx-sm bx-spin-hover'></i>
                <span class="text">Return Orders</span>
            </a>
        </li>
        <li>
            <a href="/admin/wallet">
                <i class='bx bxs-cog bx-sm bx-spin-hover'></i>
                <span class="text">Wallet Transaction</span>
            </a>
        </li>
        <li>
            <a href="/admin/logout" class="admin-logout">
                <i class='bx bx-power-off bx-sm bx-burst-hover'></i>
                <span class="text">Logout</span>
            </a>
        </li>
    </ul>
</section>
<!-- SIDEBAR END -->

<!-- <style>
    /* 📌 Admin Dashboard Link (Visible at Top Left) */
.admin-dashboard-link {
    position: absolute;
    top: 15px;
    left: 65px;
    font-size: 18px;
    font-weight: bold;
    text-decoration: none;
    color: black;
    z-index: 1001;
}

/* 📌 Sidebar Styling (Now on the Left Side) */
#admin-sidebar {
    width: 0;
    background: #ffffff; /* White Background */
    color: black;
    position: fixed;
    height: 100vh;
    left: 0; /* Now Positioned on the Left */
    transition: all 0.3s ease-in-out;
    overflow: hidden;
    border-right: 2px solid #ddd; /* Light Border */
    padding: 0;
}

/* When Sidebar is Expanded */
#admin-sidebar.show {
    width: 250px;
    padding: 15px;
}

/* 📌 Navbar Toggle Button */
.admin-navbar-toggler {
    background: transparent;
    border: none;
    color: black;
    font-size: 24px;
    position: absolute;
    left: 20px; /* Move Toggle Button to Left */
    top: 15px;
    cursor: pointer;
    z-index: 1001;
}

.admin-navbar-toggler-icon {
    display: block;
}

/* 📌 Sidebar Menu */
.admin-side-menu {
    list-style: none;
    padding: 0;
    margin-top: 20px;
}

.admin-side-menu li {
    padding: 10px;
}

.admin-side-menu li a {
    color: black;
    text-decoration: none;
    display: flex;
    align-items: center;
}

.admin-side-menu li a:hover {
    background: rgba(0, 0, 0, 0.1);
    border-radius: 5px;
    padding: 10px;
}

/* 📌 Logout Button */
.admin-logout {
    color: red;
}

</style> 
 -->



<style>
    /* 📌 Root Variables for Consistency with Main Design */
    :root {
        --primary: #1e88e5;
        --secondary: #ff4081;
        --background: #f5f7fa;
        --card-bg: #ffffff;
        --text: #212121;
        --text-light: #757575;
        --error: #d32f2f;
        --shadow: 0 4px 12px rgba(0, 0, 0, 0.1);
        --sidebar-width-closed: 0px;
        --sidebar-width-open: 250px;
    }
    
    /* 📌 Admin Dashboard Link (Visible at Top Left) */
    .admin-dashboard-link {
        position: absolute;
        top: 15px;
        left: 165px;
        font-size: 18px;
        font-weight: bold;
        text-decoration: none;
        color: var(--text);
        z-index: 1001;
        transition: left 0.3s ease-in-out;
    }
    
    /* 📌 Sidebar Styling (Left Side) */
    #admin-sidebar {
        width: var(--sidebar-width-closed);
        background: var(--card-bg);
        color: var(--text);
        position: fixed;
        height: 100vh;
        left: 0;
        top: 0;
        transition: width 0.3s ease-in-out;
        overflow: hidden;
        border-right: 2px solid #ddd;
        padding: 0;
        box-shadow: var(--shadow);
        z-index: 1000;
    }
    
    /* When Sidebar is Expanded */
    #admin-sidebar.show {
        width: var(--sidebar-width-open);
        padding: 23px;
    }
    
    /* 📌 Navbar Toggle Button */
    .admin-navbar-toggler {
        background: transparent;
        border: none;
        color: var(--text);
        font-size: 24px;
        position: fixed;
        left: 20px;
        top: 15px;
        cursor: pointer;
        z-index: 1001;
        transition: left 0.3s ease-in-out;
    }
    
    .admin-navbar-toggler-icon {
        display: block;
    }
    
    /* 📌 Sidebar Menu */
    .admin-side-menu {
        list-style: none;
        padding: 0;
        margin-top: 25px; /* Increased to account for toggle and link */
    }
    
    .admin-side-menu li {
        padding: 1px;
    }
    
    .admin-side-menu li a {
        color: var(--text);
        text-decoration: none;
        display: flex;
        align-items: center;
        padding: 10px;
        border-radius: 8px;
        transition: background 0.3s ease, color 0.3s ease;
        font-size: 1rem;
        font-weight: 500;
    }
    
    .admin-side-menu li a:hover {
        background: rgba(0, 0, 0, 0.1);
        color: var(--primary);
    }
    
    /* 📌 Logout Button */
    .admin-logout {
        color: var(--error);
    }
    
    .admin-logout:hover {
        background: rgba(211, 47, 47, 0.1); /* Light red hover effect */
        color: #b71c1c; /* Darker red on hover */
    }
    
    /* 📌 Responsive Adjustments */
    @media (max-width: 768px) {
        /* Sidebar adjustments */
        #admin-sidebar.show {
            width: 200px; /* Slightly narrower on tablets */
        }
    
        .admin-navbar-toggler {
            left: 15px;
            font-size: 20px;
        }
    
        .admin-dashboard-link {
            left: 55px;
            font-size: 16px;
        }
    
        #admin-sidebar.show ~ .admin-dashboard-link {
            left: 215px; /* Adjust position when sidebar is open */
        }
    
        .admin-side-menu {
            margin-top: 50px;
        }
    
        .admin-side-menu li a {
            font-size: 0.9rem;
            padding: 8px;
        }
    }
    
    @media (max-width: 480px) {
        /* Sidebar becomes full-width overlay on mobile */
        #admin-sidebar {
            width: 0;
            z-index: 1002; /* Above content */
        }
    
        #admin-sidebar.show {
            width: 100%;
            padding: 10px;
        }
    
        /* Toggle button adjustments */
        .admin-navbar-toggler {
            left: 10px;
            top: 10px;
            font-size: 18px;
        }
    
        /* Hide dashboard link when sidebar is closed on mobile */
        .admin-dashboard-link {
            display: none;
        }
    
        #admin-sidebar.show ~ .admin-dashboard-link {
            display: block;
            position: static;
            font-size: 14px;
            margin: 10px 0;
            text-align: center;
        }
    
        .admin-side-menu {
            margin-top: 20px;
        }
    
        .admin-side-menu li {
            padding: 8px;
        }
    
        .admin-side-menu li a {
            font-size: 0.85rem;
            padding: 6px;
        }
    }
    </style> 

<script>
    document.addEventListener("DOMContentLoaded", function () {
    const sidebar = document.getElementById("admin-sidebar");
    const toggler = document.getElementById("adminToggler");

    // Toggle Sidebar
    toggler.addEventListener("click", function () {
        sidebar.classList.toggle("show");
    });
});

</script>